pull_request_rules:
    - name: Automatic merge on approval
      conditions:
          - "#approved-reviews-by>=2"
          - check-success=lint
          - base=main
          - label!=wip
          - -title~=(?i)wip
      actions:
          merge:
              method: merge